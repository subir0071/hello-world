name: Java CI Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Java 8 details
      run: |
        echo "Java 8 Home $JAVA_HOME_8_X64"
        echo "****************************"
        java -version
